/*
Create a type "Gender" using union type that can contain the values "male" and "female". Declare a variable myGender of this type.
*/

type Gender = 'male' | 'female';

const myGender: Gender = 'male';

export {};
