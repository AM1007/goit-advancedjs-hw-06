/* 
How would you define a variable in TypeScript that can take either a string or a number (union type)?
And also define a variable that can only take one of two string values: 'enable' or 'disable' (literal type)?
*/

let union: string | number;
let literal: 'disable' | 'enable';
